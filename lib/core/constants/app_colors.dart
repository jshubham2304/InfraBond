import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0066FF);
  static const Color primaryLight = Color(0xFFE5F0FF);

  // Secondary colors
  static const Color orangeAccent = Color(0xFFFF5C00);
  static const Color greenAccent = Color(0xFF00A86B);

  // Background colors
  static const Color background = Colors.white;
  static const Color lightBackground = Color(0xFFF5F7FA);
  static const Color searchBackground = Color(0xFFF9FAFB);

  // Text colors
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textMedium = Color(0xFF666666);
  static const Color textLight = Color(0xFF999999);

  // Border colors
  static const Color border = Color(0xFFE5E7EB);

  // Status colors
  static const Color active = Color(0xFF00A86B);

  // Chart colors
  static const Color revenue = Color(0xFF0066FF);
  static const Color ebitda = Color(0xFFE5F0FF);

  // Pros & Cons colors
  static const Color proBackground = Color(0xFFF0FFF8);
  static const Color conBackground = Color(0xFFFFF8F0);
  static const Color proIcon = Color(0xFF00A86B);
  static const Color conIcon = Color(0xFFFF9500);
}
